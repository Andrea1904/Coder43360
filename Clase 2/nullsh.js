let variablePrueba=0;

let variableAsignable = variablePrueba || "Sin valor";

let variableNull = variablePrueba??"Sin valor"

console.log( "variable asignable"+variableAsignable)
console.log( "variable null"+variableNull)